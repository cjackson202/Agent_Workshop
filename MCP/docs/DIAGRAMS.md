# MCP Workshop - Architecture & Diagrams

## 🏗️ System Architecture

### High-Level Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Workshop Architecture                        │
└─────────────────────────────────────────────────────────────────────┘

   Your Local Machine                          Azure Cloud
   
┌────────────────────────┐                 ┌──────────────────────────┐
│    MCP Server          │                 │  Azure AI Foundry        │
│  (localhost:8080)      │                 │                          │
│                        │    ┌────────┐   │  ┌────────────────────┐  │
│  ┌──────────────────┐  │    │        │   │  │  Agent Service     │  │
│  │  Tools:          │  │◄───┤  HTTP  │───┤  │  - GPT-4o-mini     │  │
│  │  - add()         │  │    │  MCP   │   │  │  - Agent Framework │  │
│  │  - subtract()    │  │    │Protocol│   │  │  - Tool Selection  │  │
│  │  - multiply()    │  │───►│        │   │  └────────────────────┘  │
│  │  - your_tool()   │  │    └────────┘   │           │              │
│  └──────────────────┘  │                 │           │              │
│         │              │                 │           ▼              │
│         │              │                 │  ┌────────────────────┐  │
│    ┌────▼────┐         │                 │  │  Model Deployment  │  │
│    │Logging  │         │                 │  │  (GPT-4o-mini)     │  │
│    │& Debug  │         │                 │  └────────────────────┘  │
│    └─────────┘         │                 └──────────────────────────┘
└────────────────────────┘                            │
         ▲                                            │
         │                                            │
         │ Test                                       │ Chat
         │                                            ▼
    ┌────┴────────┐                          ┌───────────────┐
    │test_mcp.py  │                          │  You (User)   │
    │(Local Test) │                          │  via script   │
    └─────────────┘                          └───────────────┘
```

---

## 🔄 Request Flow

### How a Tool Call Works

```
Step 1: User Input
─────────────────
User: "What is 25 + 17?"
    │
    ▼
    
Step 2: Agent Receives Query
─────────────────────────────
Azure AI Foundry Agent
├─ Analyzes user intent
├─ Discovers available tools
└─ Decides to use "add" tool
    │
    ▼
    
Step 3: MCP Tool Call
──────────────────────
Agent → HTTP Request → MCP Server
{
  "tool": "add",
  "arguments": {"a": 25, "b": 17}
}
    │
    ▼
    
Step 4: Tool Execution
──────────────────────
MCP Server (server.py)
├─ Validates input
├─ Calls add(25, 17)
├─ Logs execution
└─ Returns result: 42
    │
    ▼
    
Step 5: Response Back
──────────────────────
MCP Server → HTTP Response → Agent
{
  "result": 42
}
    │
    ▼
    
Step 6: Agent Formulates Answer
────────────────────────────────
Agent:
├─ Receives tool result
├─ Generates natural language
└─ Responds to user
    │
    ▼
    
Step 7: User Sees Result
────────────────────────
Assistant: "The sum of 25 and 17 is 42."
```

---

## 🔧 Component Breakdown

### MCP Server Components

```
server.py
├─ FastMCP Instance
│  ├─ name: "Calculator"
│  ├─ host: "0.0.0.0"
│  └─ port: 8080
│
├─ Tools (@mcp.tool())
│  ├─ add(a, b)
│  ├─ subtract(a, b)
│  └─ [your custom tools]
│
├─ Resources (@mcp.resource())
│  └─ greeting://{name}
│
└─ Transport Layer
   └─ HTTP Streamable (uvicorn)
```

### Agent Components

```
get_agent_mi.py (connects to portal-created agent)
├─ Authentication
│  └─ AzureCliCredential (Managed Identity)
│
├─ Agent Connection
│  ├─ AzureAIAgentClient
│  ├─ Project Endpoint
│  ├─ Model Deployment
│  └─ Agent ID (from portal)
│
├─ MCP Tool Configuration
│  ├─ MCPStreamableHTTPTool
│  ├─ name: "Custom MCP Server"
│  └─ url: "http://localhost:8080/mcp"
│
└─ Chat Loop
   ├─ User input
   ├─ agent.run()
   └─ Display response
```

---

## 📊 Data Flow Diagrams

### Tool Discovery Flow

```
┌─────────────────────────────────────────────────────────────┐
│              How Agents Discover Tools                       │
└─────────────────────────────────────────────────────────────┘

Agent Startup
     │
     ▼
┌─────────────────────┐
│ Connect to MCP      │
│ Server at URL       │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Send Discovery      │
│ Request             │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐     MCP Server
│ Receive Tool List   │◄────┐
│ with Schemas        │     │
└──────┬──────────────┘     │
       │                    │
       ▼                    │
┌─────────────────────┐     │
│ Parse Tool Schemas  │     │
│ - name              │     │  @mcp.tool()
│ - description       │     │  def add(a: int, b: int):
│ - parameters        │     │      """Add two numbers"""
│ - required fields   │     │      return a + b
└──────┬──────────────┘     │
       │                    │
       ▼                    │
┌─────────────────────┐     │
│ Store Available     │     │
│ Tools in Context    │     │
└──────┬──────────────┘     │
       │                    │
       ▼                    │
┌─────────────────────┐     │
│ Ready to Use Tools  │─────┘
│ When Needed         │
└─────────────────────┘
```

### Multi-Tool Chaining

```
User: "What is (10 + 5) * 2?"

Agent Reasoning:
    │
    ▼
┌───────────────────┐
│ Parse Intent:     │
│ 1. Add 10 + 5     │
│ 2. Multiply by 2  │
└────────┬──────────┘
         │
         ▼
    ┌────────────────────────────────┐
    │      Tool Call #1: add         │
    │  ┌──────────────────────────┐  │
    │  │ call add(10, 5)          │  │
    │  │ → result: 15             │  │
    │  └──────────────────────────┘  │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │    Tool Call #2: multiply      │
    │  ┌──────────────────────────┐  │
    │  │ call multiply(15, 2)     │  │
    │  │ → result: 30             │  │
    │  └──────────────────────────┘  │
    └────────┬───────────────────────┘
             │
             ▼
    ┌────────────────────────────────┐
    │   Formulate Final Answer       │
    │  "The result is 30"            │
    └────────────────────────────────┘
```

---

## 🔐 Security Architecture

### Authentication Flow

```
┌──────────────────────────────────────────────────────────────┐
│              Managed Identity Authentication                  │
└──────────────────────────────────────────────────────────────┘

Your Machine                    Azure Active Directory
     │                                   │
     │  1. az login                     │
     ├──────────────────────────────────►│
     │                                   │
     │  2. Token granted                │
     │◄──────────────────────────────────┤
     │                                   │
     │                                   │
Your Script                    Azure AI Foundry
     │                                   │
     │  3. AzureCliCredential()         │
     │     uses cached token            │
     │                                   │
     │  4. Create agent request         │
     │     with token                   │
     ├──────────────────────────────────►│
     │                                   │
     │  5. Validate token               │
     │     & authorize                  │
     │                                   │
     │  6. Agent created                │
     │◄──────────────────────────────────┤
     │                                   │
     
Note: MCP Server (localhost) has NO authentication in this workshop.
      Add auth (API key, OAuth) before production deployment!
```

---

## 🎯 Workshop Exercise Flow

### Visual Exercise Progression

```
Exercise 1: Understanding
┌────────────────────────┐
│  Read server.py        │
│  Understand @mcp.tool()│
│  Add multiply() tool   │
└───────────┬────────────┘
            │
            ▼
Exercise 2: Local Testing
┌────────────────────────┐
│  Start server.py       │
│  Run test_mcp.py       │
│  Verify tools work     │
└───────────┬────────────┘
            │
            ▼
Exercise 3: Azure Integration
┌────────────────────────┐
│  Create agent in       │
│  Azure portal          │
│  Connect with          │
│  get_agent_mi.py       │
└───────────┬────────────┘
            │
            ▼
Exercise 4: Custom Tools
┌────────────────────────┐
│  Design your tool      │
│  Implement & test      │
│  Use with agent        │
└────────────────────────┘
│  Use with agent        │
└────────────────────────┘
```

---

## 🌐 Multi-Agent Architecture

### One Server, Multiple Agents

```
┌──────────────────────────────────────────────────────────────┐
│                  MCP Server (Port 8080)                       │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │Tool 1   │  │Tool 2   │  │Tool 3   │  │Tool N   │        │
│  │add()    │  │query_db│  │send_mail│  │...      │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
└───┬───────────┬───────────┬───────────┬──────────────────────┘
    │           │           │           │
    │ MCP       │ MCP       │ MCP       │ MCP
    │           │           │           │
┌───▼─────┐ ┌───▼─────┐ ┌───▼─────┐ ┌───▼─────┐
│ Agent 1 │ │ Agent 2 │ │ Agent 3 │ │ Agent 4 │
│Customer │ │ Data    │ │Report   │ │Monitor  │
│Service  │ │Analysis │ │Gen      │ │         │
└─────────┘ └─────────┘ └─────────┘ └─────────┘

Benefits:
✓ Shared tool library
✓ Centralized updates
✓ Consistent behavior
✓ Easier maintenance
```

---

## 📦 Deployment Architecture

### Development (Workshop)

```
┌─────────────────────┐
│  Your Laptop        │
│  ┌───────────────┐  │
│  │ MCP Server    │  │
│  │ localhost:8080│  │
│  └───────────────┘  │
└─────────────────────┘
         │
         │ HTTP
         │
    ┌────▼────┐
    │ Azure   │
    │ Agent   │
    └─────────┘
```

### Production

```
┌──────────────────────────────────────────┐
│         Azure Cloud                      │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  Azure API Management              │ │
│  │  - Rate limiting                   │ │
│  │  - Authentication                  │ │
│  │  - Monitoring                      │ │
│  └────────────┬───────────────────────┘ │
│               │                          │
│  ┌────────────▼───────────────────────┐ │
│  │  Azure Container Apps              │ │
│  │  ┌──────────────────────────────┐  │ │
│  │  │  MCP Server Containers       │  │ │
│  │  │  - Auto-scaling              │  │ │
│  │  │  - Load balancing            │  │ │
│  │  │  - Health monitoring         │  │ │
│  │  └──────────────────────────────┘  │ │
│  └────────────┬───────────────────────┘ │
│               │                          │
│  ┌────────────▼───────────────────────┐ │
│  │  Azure AI Foundry Agents           │ │
│  │  - Multiple agent instances        │ │
│  │  - Shared tool access              │ │
│  └────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

---

## 🔍 Debugging Flow

### When Things Go Wrong

```
Issue: Agent not using tool
    │
    ▼
┌─────────────────────────┐
│ Check: Is server        │
│ running?                │
└──────┬──────────────────┘
       │ YES
       ▼
┌─────────────────────────┐
│ Check: Does test_mcp.py │
│ show the tool?          │
└──────┬──────────────────┘
       │ YES
       ▼
┌─────────────────────────┐
│ Check: Tool has         │
│ @mcp.tool() decorator?  │
└──────┬──────────────────┘
       │ YES
       ▼
┌─────────────────────────┐
│ Check: Docstring        │
│ present?                │
└──────┬──────────────────┘
       │ YES
       ▼
┌─────────────────────────┐
│ Check: Agent            │
│ instructions clear?     │
└──────┬──────────────────┘
       │ YES
       ▼
┌─────────────────────────┐
│ Check: Server logs      │
│ for errors              │
└──────┬──────────────────┘
       │
       ▼
    [Debug deeper or
     ask for help]
```

---

## 📊 Comparison Diagrams

### Portal Creation vs Programmatic

```
Portal Creation                     get_agent_mi.py
(Azure AI Foundry Studio)          (Connection Script)
        │                                 │
        ▼                                 ▼
┌───────────────────┐           ┌───────────────────┐
│ Create agent in   │           │ Connect to        │
│ Azure portal UI   │           │ existing agent    │
└────────┬──────────┘           └────────┬──────────┘
         │                               │
         ▼                               ▼
┌───────────────────┐           ┌───────────────────┐
│ Visible in UI     │           │ Add MCP tools     │
│ Easy management   │           │ to portal agent   │
│ Copy agent ID     │           │ (requires ID)     │
└────────┬──────────┘           └────────┬──────────┘
         │                               │
         ▼                               ▼
┌───────────────────┐           ┌───────────────────┐
│ Use for:          │           │ Use for:          │
│ - Agent creation  │           │ - Tool integration│
│ - Configuration   │           │ - Testing MCP     │
│ - Monitoring      │           │ - Production use  │
└───────────────────┘           └───────────────────┘

```

---

## 🎯 Tool Lifecycle

```
1. Definition
   │
   │  @mcp.tool()
   │  def my_tool(param: type) -> type:
   │      """Description"""
   │      return result
   │
   ▼
2. Registration
   │
   │  Server starts
   │  FastMCP registers tool
   │  Schema generated
   │
   ▼
3. Discovery
   │
   │  Agent connects
   │  Requests tool list
   │  Receives schemas
   │
   ▼
4. Selection
   │
   │  User asks question
   │  Agent analyzes intent
   │  Decides which tool
   │
   ▼
5. Invocation
   │
   │  Agent calls tool via HTTP
   │  Parameters validated
   │  Function executed
   │
   ▼
6. Return
   │
   │  Result sent back
   │  Agent receives data
   │  Formulates response
   │
   ▼
7. Response
   │
   │  User sees answer
   │  Tool logged
   │  Session continues
```

---

## 🎨 Visual Metaphor

### MCP as a Tool Shed

```
        🏠 Your House (Agent)
           │
           │ "I need a hammer"
           │
           ▼
    ┌─────────────────────┐
    │   🚪 Tool Shed Door  │  ← MCP Protocol
    │    (Standardized)    │
    └──────────┬───────────┘
               │
               ▼
    ┌─────────────────────────────────┐
    │      🛠️ Tool Shed (MCP Server)  │
    │                                  │
    │  🔨 Hammer (add)                │
    │  🪛 Screwdriver (subtract)      │
    │  📏 Tape Measure (multiply)     │
    │  ⚡ Drill (your custom tool)    │
    │                                  │
    │  All organized, documented,     │
    │  and ready to use!              │
    └─────────────────────────────────┘

Without MCP: Every house needs its own tools
With MCP: One shared, well-organized tool shed
```

---

**Use these diagrams in your presentations, documentation, or when explaining concepts!**
